import { ArrowLeft, CheckCircle2, Database, FileUp, Image as ImageIcon, Send, ShieldCheck, Trash2 } from "lucide-react";
import { useMemo, useState, type ChangeEvent, type FormEvent } from "react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";

type UploadKind = "photo" | "data" | "report" | "protocol" | "other";
type UploadDraft = { fileName: string; data: string; mimeType: string; sizeBytes: number; kind: UploadKind };
const allowedTypes = new Set(["image/jpeg", "image/png", "application/pdf", "text/csv", "application/vnd.ms-excel", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"]);
const kindFor = (file: File): UploadKind => file.type.startsWith("image/") ? "photo" : /csv|excel|spreadsheet/.test(file.type) ? "data" : "report";

export default function EvidenceSubmission() {
  const targets = trpc.experiments.completed.useQuery();
  const [submitterName, setSubmitterName] = useState("");
  const [experimentId, setExperimentId] = useState("");
  const [observationNotes, setObservationNotes] = useState("");
  const [uploads, setUploads] = useState<UploadDraft[]>([]);
  const [fileError, setFileError] = useState("");
  const [preparing, setPreparing] = useState(false);
  const [validationError, setValidationError] = useState("");
  const submit = trpc.submissions.evidence.useMutation();
  const selected = useMemo(() => targets.data?.find((item) => String(item.id) === experimentId), [experimentId, targets.data]);

  const onFilesChange = (event: ChangeEvent<HTMLInputElement>) => {
    const files = Array.from(event.target.files ?? []);
    if (!files.length) return;
    setFileError("");
    if (files.length + uploads.length > 8) { setFileError("You can attach up to 8 files."); return; }
    const invalid = files.find((file) => !allowedTypes.has(file.type) || file.size > 8 * 1024 * 1024);
    if (invalid) { setFileError(`${invalid.name} is not a JPG, PNG, CSV, XLSX, or PDF file under 8 MB.`); return; }
    setPreparing(true);
    Promise.all(files.map((file) => new Promise<UploadDraft>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve({ fileName: file.name, data: typeof reader.result === "string" ? reader.result : "", mimeType: file.type, sizeBytes: file.size, kind: kindFor(file) }); reader.onerror = () => reject(new Error(`Could not read ${file.name}`)); reader.readAsDataURL(file); }))).then((next) => setUploads((current) => [...current, ...next])).catch((error: Error) => setFileError(error.message)).finally(() => { setPreparing(false); event.target.value = ""; });
  };
  const onSubmit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setValidationError(""); if (!submitterName.trim() || !experimentId) { setValidationError("Submitter Name and a target completed experiment are required."); return; } if (preparing) { setValidationError("Please wait for the files to finish preparing."); return; } submit.mutate({ submitterName, experimentId: Number(experimentId), observationNotes, files: uploads }); };
  if (submit.isSuccess) return <div className="mx-auto max-w-[1440px] px-5 py-16 lg:px-10 lg:py-24"><div className="mx-auto max-w-2xl border border-[#b8ccb5] bg-[#edf5eb] p-8 text-center md:p-14"><CheckCircle2 className="mx-auto text-[#3f7b44]" size={36} /><div className="mt-5 font-mono text-[10px] uppercase tracking-[.2em] text-[#3f7b44]">Evidence supplement received</div><h1 className="mt-4 font-display text-4xl tracking-[-.04em]">Your evidence is awaiting review.</h1><p className="mx-auto mt-5 max-w-lg text-sm leading-7 text-muted-foreground">It will not change the public experiment until an administrator approves and attaches it to the selected completed project.</p><Link href="/" className="focus-ring mt-8 inline-flex bg-primary px-4 py-3 font-mono text-[10px] uppercase tracking-[.14em] text-white">Return to overview</Link></div></div>;
  return <div><section className="navy-grid text-white"><div className="mx-auto max-w-[1440px] px-5 pb-14 pt-14 lg:px-10 lg:pb-18 lg:pt-20"><Link href="/" className="focus-ring inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.18em] text-white/45 hover:text-signal"><ArrowLeft size={13} /> Back to overview</Link><div className="mt-10 font-mono text-[10px] uppercase tracking-[.2em] text-signal">Completed project / Evidence supplement</div><h1 className="mt-4 max-w-4xl font-display text-[clamp(2.8rem,6vw,5.8rem)] leading-[1.02] tracking-[-.06em]">Add what<br /><span className="text-[#9dc4f4]">actually happened.</span></h1><p className="mt-6 max-w-2xl text-[16px] leading-7 text-white/60">Upload real-run photos, raw data, and observation notes for an experiment that is already archived in Completed Experimental Projects.</p></div></section><div className="mx-auto grid max-w-[1440px] gap-10 px-5 py-12 lg:grid-cols-[1fr_310px] lg:px-10 lg:py-16"><form onSubmit={onSubmit} className="space-y-8"><section className="border border-border bg-card p-5 md:p-7"><div className="flex items-start gap-4 border-b border-border pb-5"><div className="flex h-8 w-8 items-center justify-center bg-[#e7eef6] font-mono text-[10px] text-primary">01</div><div><h2 className="font-display text-xl">Identify the supplement</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">These are the only two mandatory fields.</p></div></div><div className="mt-6 grid gap-5 md:grid-cols-2"><label><span className="form-label">Submitter Name *</span><input value={submitterName} onChange={(e) => setSubmitterName(e.target.value)} className="form-control mt-2" placeholder="e.g. Jordan Lee" /></label><label><span className="form-label">Target Archived Experiment *</span><select value={experimentId} onChange={(e) => setExperimentId(e.target.value)} className="form-control mt-2" disabled={targets.isLoading}><option value="">{targets.isLoading ? "Loading completed projects…" : "Select a completed experiment"}</option>{(targets.data ?? []).map((item) => <option key={item.id} value={item.id}>{item.title} · {item.discipline}</option>)}</select></label></div>{targets.error && <p className="mt-4 text-sm text-[#8a2c2c]">Completed projects could not be loaded. Please refresh and try again.</p>}</section><section className="border border-border bg-card p-5 md:p-7"><div className="flex items-start gap-4 border-b border-border pb-5"><div className="flex h-8 w-8 items-center justify-center bg-[#e7eef6] font-mono text-[10px] text-primary">02</div><div><h2 className="font-display text-xl">Execution observation notes</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Optional qualitative notes from the real-world run.</p></div></div><textarea value={observationNotes} onChange={(e) => setObservationNotes(e.target.value)} className="form-control mt-6 min-h-40 resize-y" placeholder="What happened during the session? Note unexpected behavior, participant reactions, timing, or context..." /></section><section className="border border-[#9fb0c4] bg-[#eef4f9] p-5 md:p-7"><div className="flex items-start gap-4"><div className="flex h-8 w-8 items-center justify-center bg-white text-primary">03</div><div><h2 className="font-display text-xl">Session evidence</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Optional: add up to 8 files total, including multiple photos. Each file may be up to 8 MB.</p></div></div><label className="focus-ring mt-6 flex min-h-16 cursor-pointer items-center gap-3 border border-dashed border-[#7897b8] bg-white px-4 py-3 text-sm text-muted-foreground hover:border-primary hover:text-primary"><FileUp size={18} /><span><strong className="block font-medium text-ink">Add multiple photos or data files</strong><span className="text-xs">Select several files at once, or add more files again later · JPG · PNG · CSV · XLSX · PDF</span></span><input type="file" multiple accept="image/jpeg,image/png,.csv,.xlsx,.xls,.pdf" className="sr-only" onChange={onFilesChange} /></label>{uploads.length > 0 && <div className="mt-4 grid gap-2 md:grid-cols-2">{uploads.map((file, index) => <div key={`${file.fileName}-${index}`} className="flex items-center gap-3 border border-[#c7d4e1] bg-white p-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center bg-[#e7eef6] text-primary">{file.kind === "photo" ? <ImageIcon size={15} /> : <Database size={15} />}</span><span className="min-w-0 flex-1"><strong className="block truncate text-sm text-ink">{file.fileName}</strong><span className="font-mono text-[9px] uppercase tracking-[.12em] text-muted-foreground">{file.kind} · {(file.sizeBytes / 1024 / 1024).toFixed(2)} MB</span></span><button type="button" onClick={() => setUploads((current) => current.filter((_, i) => i !== index))} className="text-muted-foreground hover:text-[#8a2c2c]" aria-label={`Remove ${file.fileName}`}><Trash2 size={15} /></button></div>)}</div>}</section>{(validationError || fileError || submit.error) && <div role="alert" className="border border-[#d9a7a7] bg-[#fff1f1] px-4 py-3 text-sm text-[#8a2c2c]">{validationError || fileError || submit.error?.message}</div>}<div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-2 font-mono text-[9px] uppercase tracking-[.12em] text-muted-foreground"><ShieldCheck size={14} className="text-[#3f7b44]" /> Saved privately until admin approval</div><button type="submit" disabled={submit.isPending || preparing} className="focus-ring flex h-12 items-center gap-2 bg-primary px-6 font-mono text-[10px] uppercase tracking-[.14em] text-white hover:bg-[#083d80]">{preparing ? "Preparing files…" : submit.isPending ? "Sending…" : "Submit evidence"} <Send size={14} /></button></div></form><aside className="h-fit border border-border bg-[#ece9e2] p-6 lg:sticky lg:top-24"><div className="font-mono text-[10px] uppercase tracking-[.18em] text-primary">Evidence protocol</div><div className="mt-6 space-y-5 text-sm leading-6 text-muted-foreground"><p><strong className="font-medium text-ink">This is not a new experiment.</strong><br />Choose an existing completed project only.</p><p><strong className="font-medium text-ink">Admin review first.</strong><br />Approved evidence is appended to that project's execution records.</p><p><strong className="font-medium text-ink">Rejected evidence stays private.</strong><br />The submission is retained for administrative history but never shown publicly.</p></div></aside></div></div>;
}
