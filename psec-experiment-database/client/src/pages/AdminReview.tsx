import { Check, ChevronDown, Clock3, Download, Edit3, FileUp, History, LockKeyhole, LogOut, RotateCcw, Search, ShieldAlert, X } from "lucide-react";
import { useMemo, useState, type FormEvent } from "react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";

const libraryDisciplines = ["Social Psychology", "Behavioral Economics", "Sociology", "Moral & Political Philosophy"];
const libraryFolders = ["Idea Pool", "Formal Experimental Designs", "Completed Experimental Projects"];

const emptyEdit = {
  memberName: "",
  memberId: "",
  discipline: "Social Psychology",
  title: "",
  abstract: "",
  lifecycle: "idea",
  theoreticalBasis: "",
  historicalBackground: "",
  hypothesis: "",
  procedure: "",
  materials: "",
  expectedOutput: "",
  attachmentName: "",
  attachmentData: "",
  attachmentMimeType: "",
};

type EditState = typeof emptyEdit;

export default function AdminReview() {
  const status = trpc.admin.status.useQuery();
  const [password, setPassword] = useState("");
  const [loginError, setLoginError] = useState("");
  const [disciplineFilter, setDisciplineFilter] = useState("");
  const [fromFilter, setFromFilter] = useState("");
  const [toFilter, setToFilter] = useState("");
  const [openId, setOpenId] = useState<number | null>(null);
  const [approveId, setApproveId] = useState<number | null>(null);
  const [rejectId, setRejectId] = useState<number | null>(null);
  const [rejectComment, setRejectComment] = useState("");
  const [editId, setEditId] = useState<number | null>(null);
  const [editForm, setEditForm] = useState<EditState>(emptyEdit);
  const [notice, setNotice] = useState("");
  const utils = trpc.useUtils();

  const filters = useMemo(() => ({ discipline: disciplineFilter || undefined, from: fromFilter || undefined, to: toFilter || undefined }), [disciplineFilter, fromFilter, toFilter]);
  const queue = trpc.admin.pending.useQuery(filters, { enabled: status.data?.authenticated === true });
  const history = trpc.admin.history.useQuery({ submissionId: openId || 1 }, { enabled: status.data?.authenticated === true && openId !== null });
  const login = trpc.admin.login.useMutation({ onSuccess: (result) => { if (result.success) { setLoginError(""); void utils.admin.status.invalidate(); } else setLoginError("Password not recognized. Try again."); }, onError: (error) => setLoginError(error.message) });
  const logout = trpc.admin.logout.useMutation({ onSuccess: () => window.location.assign("/") });
  const approve = trpc.admin.approve.useMutation({ onSuccess: () => { setApproveId(null); setNotice("Submission approved and published to the selected library folder."); void utils.admin.pending.invalidate(); void utils.experiments.list.invalidate(); }, onError: (error) => setNotice(`Error: ${error.message}`) });
  const reject = trpc.admin.reject.useMutation({ onSuccess: () => { setRejectId(null); setRejectComment(""); setNotice("Submission rejected and retained with its comment and history."); void utils.admin.pending.invalidate(); }, onError: (error) => setNotice(`Error: ${error.message}`) });
  const edit = trpc.admin.edit.useMutation({ onSuccess: () => { setEditId(null); setNotice("Edit appended to the iteration history without deleting the original snapshot."); void utils.admin.pending.invalidate(); void utils.admin.history.invalidate(); }, onError: (error) => setNotice(`Error: ${error.message}`) });

  const submitPassword = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    setLoginError("");
    login.mutate({ password });
  };
  const setEditValue = (key: keyof EditState, value: string) => setEditForm((current) => ({ ...current, [key]: value }));
  const beginEdit = (item: QueueItem) => {
    setEditId(item.id);
    setEditForm({ memberName: item.memberName || "", memberId: item.memberId || "", discipline: item.discipline || "", title: item.title || "", abstract: item.abstract || "", lifecycle: item.lifecycle || "idea", theoreticalBasis: item.theoreticalBasis || "", historicalBackground: item.historicalBackground || "", hypothesis: item.hypothesis || "", procedure: item.procedure || "", materials: item.materials || "", expectedOutput: item.expectedOutput || "", attachmentName: item.attachmentName || "", attachmentData: "", attachmentMimeType: "" });
  };
  const submitEdit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    edit.mutate({ id: editId || 0, values: { ...editForm, lifecycle: editForm.lifecycle as "idea" | "design" | "in_progress" | "completed" }, note: "Admin edited the current working copy; original and prior versions remain in history." });
  };

  if (status.isLoading) {
    return <div className="flex min-h-screen items-center justify-center bg-[#f5f2eb]" role="status"><span className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Checking protected session…</span></div>;
  }

  if (!status.data?.authenticated) {
    return <div className="min-h-[calc(100vh-74px)] bg-[#f5f2eb] px-5 py-16 lg:px-10 lg:py-24"><div className="mx-auto max-w-md border border-border bg-card p-8 md:p-10"><div className="flex h-11 w-11 items-center justify-center bg-[#e7eef6] text-primary"><LockKeyhole size={20} /></div><div className="mt-8 font-mono text-[10px] uppercase tracking-[.2em] text-primary">Restricted route / PSEC internal</div><h1 className="mt-3 font-display text-3xl tracking-[-.04em]">Admin Review Queue</h1><p className="mt-4 text-sm leading-6 text-muted-foreground">This queue is not linked from the public website. Enter an authorized admin password to continue.</p><form onSubmit={submitPassword} className="mt-7"><label className="font-mono text-[10px] uppercase tracking-[.14em] text-ink">Admin password<input autoFocus type="password" value={password} onChange={(event) => setPassword(event.target.value)} className="form-control mt-2" placeholder="Enter password" /></label>{loginError && <div className="mt-3 flex items-center gap-2 text-sm text-[#8a2c2c]"><ShieldAlert size={15} /> {loginError}</div>}<button type="submit" disabled={login.isPending} className="focus-ring mt-5 flex h-11 w-full items-center justify-center gap-2 bg-primary font-mono text-[10px] uppercase tracking-[.14em] text-white hover:bg-[#083d80]">{login.isPending ? "Checking…" : "Open queue"} <LockKeyhole size={14} /></button></form><Link href="/" className="mt-6 block text-center font-mono text-[10px] uppercase tracking-[.14em] text-muted-foreground hover:text-primary">Return to public homepage</Link></div></div>;
  }

  const items = (queue.data ?? []) as QueueItem[];
  return <div className="min-h-screen bg-[#f5f2eb]">
    <section className="navy-grid text-white"><div className="mx-auto max-w-[1440px] px-5 pb-12 pt-12 lg:flex lg:items-end lg:justify-between lg:px-10 lg:pb-16 lg:pt-16"><div><div className="flex items-center gap-3 font-mono text-[10px] uppercase tracking-[.2em] text-signal"><span className="h-px w-8 bg-signal" /> Restricted workspace</div><h1 className="mt-4 font-display text-[clamp(2.7rem,5vw,5rem)] leading-[1.02] tracking-[-.06em]">Admin Review Queue</h1><p className="mt-5 max-w-2xl text-sm leading-6 text-white/60">Only unreviewed pending submissions appear here. Approval is the single action that moves a record into a public library.</p></div><button onClick={() => logout.mutate()} className="focus-ring mt-8 flex items-center gap-2 border border-white/20 px-4 py-3 font-mono text-[10px] uppercase tracking-[.15em] text-white/75 hover:border-signal hover:text-signal lg:mt-0"><LogOut size={14} /> LOGOUT</button></div></section>
    <main className="mx-auto max-w-[1440px] px-5 py-10 lg:px-10 lg:py-14">
      <ExperimentAdminPanel />
      <EvidenceQueue />
      {notice && <div role={notice.startsWith("Error:") ? "alert" : "status"} className={`mb-6 flex items-center justify-between gap-4 border px-4 py-3 text-sm ${notice.startsWith("Error:") ? "border-[#d9a7a7] bg-[#fff1f1] text-[#8a2c2c]" : "border-[#b8ccb5] bg-[#edf5eb] text-[#3f7b44]"}`}><span>{notice}</span><button onClick={() => setNotice("")} aria-label="Dismiss notice"><X size={15} /></button></div>}
      <section className="border border-border bg-card p-5 md:p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-primary">Pending only</div><h2 className="mt-2 font-display text-2xl">{items.length} record{items.length === 1 ? "" : "s"} awaiting a decision</h2></div><div className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground"><Clock3 size={14} className="mr-1 inline" /> No automatic publishing</div></div><div className="mt-6 grid gap-3 md:grid-cols-[1fr_1fr_1fr_auto]"><label className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground">Academic category<select value={disciplineFilter} onChange={(event) => setDisciplineFilter(event.target.value)} className="form-control mt-1"><option value="">All categories</option>{libraryDisciplines.map((item) => <option key={item}>{item}</option>)}</select></label><label className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground">Submitted from<input type="date" value={fromFilter} onChange={(event) => setFromFilter(event.target.value)} className="form-control mt-1" /></label><label className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground">Submitted to<input type="date" value={toFilter} onChange={(event) => setToFilter(event.target.value)} className="form-control mt-1" /></label><button onClick={() => { setDisciplineFilter(""); setFromFilter(""); setToFilter(""); }} className="focus-ring self-end border border-border bg-white px-4 py-3 font-mono text-[10px] uppercase tracking-[.13em] text-muted-foreground hover:text-primary"><RotateCcw size={13} className="mr-1 inline" /> Reset</button></div></section>
      <section className="mt-7 space-y-5">{queue.isLoading && <div className="border border-border bg-card p-8 font-mono text-[10px] uppercase tracking-[.15em] text-muted-foreground">Loading pending submissions…</div>}{!queue.isLoading && items.length === 0 && <div className="border border-dashed border-[#aeb9c8] bg-[#f0f4f8] p-12 text-center"><Search className="mx-auto text-primary" size={22} /><p className="mt-4 font-display text-xl">Nothing is waiting in the queue.</p><p className="mt-2 text-sm text-muted-foreground">New member submissions will appear here after they are saved.</p></div>}{items.map((item) => <SubmissionCard key={item.id} item={item} openId={openId} setOpenId={setOpenId} approveId={approveId} setApproveId={setApproveId} rejectId={rejectId} setRejectId={setRejectId} rejectComment={rejectComment} setRejectComment={setRejectComment} editId={editId} setEditId={setEditId} editForm={editForm} setEditValue={setEditValue} submitEdit={submitEdit} beginEdit={beginEdit} history={history.data ?? []} onApprove={(discipline) => approve.mutate({ id: item.id, discipline })} onReject={() => reject.mutate({ id: item.id, comment: rejectComment })} approving={approve.isPending} rejecting={reject.isPending} editing={edit.isPending} />)}</section>
    </main>
  </div>;
}

function EvidenceQueue() {
  const utils = trpc.useUtils();
  const queue = trpc.admin.pendingEvidence.useQuery();
  const [rejectId, setRejectId] = useState<number | null>(null);
  const [comment, setComment] = useState("");
  const [message, setMessage] = useState("");
  const approve = trpc.admin.approveEvidence.useMutation({ onSuccess: () => { setMessage("Evidence approved and attached to the selected completed project."); void utils.admin.pendingEvidence.invalidate(); void utils.experiments.executionRecords.invalidate(); void utils.experiments.attachments.invalidate(); }, onError: (error) => setMessage(`Error: ${error.message}`) });
  const reject = trpc.admin.rejectEvidence.useMutation({ onSuccess: () => { setRejectId(null); setComment(""); setMessage("Evidence rejected and retained privately in the backend."); void utils.admin.pendingEvidence.invalidate(); }, onError: (error) => setMessage(`Error: ${error.message}`) });
  return <section className="mb-7 border border-[#9fb0c4] bg-[#eef4f9] p-5 md:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-primary">Evidence supplements / separate queue</div><h2 className="mt-2 font-display text-2xl">Completed-experiment evidence</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">These submissions add real-run photos, raw data, and observation notes to an existing completed project. They never create a new experiment page.</p></div><div className="font-mono text-[10px] uppercase tracking-[.13em] text-primary">{queue.data?.length ?? 0} pending</div></div>{message && <div role={message.startsWith("Error:") ? "alert" : "status"} className={`mt-4 border px-4 py-3 text-sm ${message.startsWith("Error:") ? "border-[#d9a7a7] bg-[#fff1f1] text-[#8a2c2c]" : "border-[#b8ccb5] bg-[#edf5eb] text-[#3f7b44]"}`}>{message}</div>}<div className="mt-5 space-y-3">{queue.isLoading ? <div className="font-mono text-[10px] uppercase tracking-[.14em] text-muted-foreground">Loading evidence supplements…</div> : queue.data?.length === 0 ? <div className="border border-dashed border-[#9fb0c4] bg-white p-5 text-sm text-muted-foreground">No evidence supplements are waiting.</div> : queue.data?.map((item) => <article key={item.id} className="border border-[#c7d4e1] bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="inline-flex bg-[#e7eef6] px-2 py-1 font-mono text-[9px] uppercase tracking-[.13em] text-primary">[EVIDENCE SUPPLEMENT]</span><h3 className="mt-2 font-display text-xl">{item.experimentTitle || "Target experiment unavailable"}</h3><p className="mt-1 text-xs text-muted-foreground">Submitter: <strong className="text-ink">{item.submitterName}</strong> · {new Date(item.submittedAt).toLocaleString()}</p></div><div className="flex gap-2"><button onClick={() => approve.mutate({ id: item.id })} disabled={approve.isPending || !item.experimentTitle} className="focus-ring bg-[#3f7b44] px-3 py-2 font-mono text-[10px] uppercase tracking-[.11em] text-white disabled:opacity-40">{approve.isPending ? "Attaching…" : "Approve & Attach"}</button><button onClick={() => setRejectId(rejectId === item.id ? null : item.id)} className="focus-ring border border-[#c58e8e] bg-[#fff5f5] px-3 py-2 font-mono text-[10px] uppercase tracking-[.11em] text-[#8a2c2c]">Reject</button></div></div>{item.observationNotes && <div className="mt-4 border-l-2 border-primary pl-3 text-sm leading-6 text-muted-foreground"><span className="font-mono text-[9px] uppercase tracking-[.13em] text-primary">Observation notes</span><p className="mt-1 whitespace-pre-wrap">{item.observationNotes}</p></div>}{item.attachments.length > 0 && <div className="mt-4 flex flex-wrap gap-2">{item.attachments.map((file) => <a key={file.id} href={file.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 border border-[#d9d7d1] px-2 py-1 text-xs text-primary underline"><Download size={12} /> {file.fileName}</a>)}</div>}{rejectId === item.id && <div className="mt-4 border-t border-[#ead0d0] pt-4"><label className="block text-sm text-[#8a2c2c]">Rejection comment<textarea value={comment} onChange={(event) => setComment(event.target.value)} className="form-control mt-1 min-h-24 resize-y" placeholder="Explain what should change before resubmission..." /></label><button onClick={() => reject.mutate({ id: item.id, comment })} disabled={!comment.trim() || reject.isPending} className="focus-ring mt-3 bg-[#8a2c2c] px-3 py-2 font-mono text-[10px] uppercase tracking-[.11em] text-white">{reject.isPending ? "Saving…" : "Confirm rejection"}</button></div>}</article>)}</div></section>;
}

type QueueItem = {
  id: number; memberName: string; memberId: string; discipline: string; title: string; abstract: string | null; lifecycle: "idea" | "design" | "in_progress" | "completed" | null; theoreticalBasis: string | null; historicalBackground: string | null; hypothesis: string | null; procedure: string | null; materials: string | null; expectedOutput: string | null; attachmentName: string | null; attachmentUrl: string | null; attachments?: { id: number; fileName: string; url: string; mimeType: string; sizeBytes: number | null; kind: string }[]; submittedAt: string | Date; status: string;
};

type HistoryItem = { id: number; submissionId: number; action: string; editor: string; note: string | null; snapshotJson: string; createdAt: string | Date };

function SubmissionCard({ item, openId, setOpenId, approveId, setApproveId, rejectId, setRejectId, rejectComment, setRejectComment, editId, setEditId, editForm, setEditValue, submitEdit, beginEdit, history, onApprove, onReject, approving, rejecting, editing }: { item: QueueItem; openId: number | null; setOpenId: (id: number | null) => void; approveId: number | null; setApproveId: (id: number | null) => void; rejectId: number | null; setRejectId: (id: number | null) => void; rejectComment: string; setRejectComment: (value: string) => void; editId: number | null; setEditId: (id: number | null) => void; editForm: EditState; setEditValue: (key: keyof EditState, value: string) => void; submitEdit: (event: FormEvent<HTMLFormElement>) => void; beginEdit: (item: QueueItem) => void; history: HistoryItem[]; onApprove: (discipline: string) => void; onReject: () => void; approving: boolean; rejecting: boolean; editing: boolean }) {
  const [approveDiscipline, setApproveDiscipline] = useState(item.discipline || "Social Psychology");
  const [approveFolder, setApproveFolder] = useState("Idea Pool");
  const expanded = openId === item.id;
  const historyForItem = history.filter((record) => record.submissionId === item.id);
  const submittedAt = new Date(item.submittedAt).toLocaleString();
  const display = (value: string | null) => value?.trim() || "— blank —";

  return <article className="border border-border bg-card"><button onClick={() => setOpenId(expanded ? null : item.id)} className="focus-ring flex w-full items-start gap-4 p-5 text-left md:p-6"><span className="flex h-9 w-9 shrink-0 items-center justify-center bg-[#f4e7c4] font-mono text-[10px] text-[#856311]">#{item.id}</span><span className="min-w-0 flex-1"><span className="flex flex-wrap items-center gap-2 font-mono text-[9px] uppercase tracking-[.13em] text-primary"><span>{item.discipline || "Uncategorized"}</span><span className="h-1 w-1 rounded-full bg-signal" /><span>{submittedAt}</span></span><span className="mt-2 block font-display text-2xl leading-8 tracking-[-.03em]">{item.title}</span><span className="mt-2 block text-sm text-muted-foreground">Submitter: <strong className="font-medium text-ink">{display(item.memberName)} / {display(item.memberId)}</strong></span></span><ChevronDown size={18} className={`mt-1 shrink-0 text-muted-foreground transition-transform ${expanded ? "rotate-180 text-primary" : ""}`} /></button>{expanded && <div className="border-t border-border bg-[#f7f5ef] p-5 md:p-7"><div className="grid gap-7 lg:grid-cols-2"><div className="space-y-5"><RecordField label="Member Name" value={item.memberName} /><RecordField label="Member ID" value={item.memberId} /><RecordField label="Academic Category" value={item.discipline} /><RecordField label="Experiment / Idea Title" value={item.title} /><RecordField label="One-sentence summary" value={item.abstract} /><RecordField label="Research lifecycle" value={item.lifecycle} /><RecordField label="Theoretical Basis" value={item.theoreticalBasis} /><RecordField label="Creator & Full Historical Background" value={item.historicalBackground} /></div><div className="space-y-5"><RecordField label="Research Hypothesis" value={item.hypothesis} /><RecordField label="Proposed Experimental Procedure" value={item.procedure} /><RecordField label="Required Materials & Environment" value={item.materials} /><RecordField label="Expected Academic Output" value={item.expectedOutput} /><RecordField label="Submission Timestamp" value={submittedAt} /><div><div className="font-mono text-[9px] uppercase tracking-[.15em] text-primary">Attached file</div>{item.attachmentUrl ? <a href={item.attachmentUrl} target="_blank" rel="noreferrer" className="mt-2 inline-flex items-center gap-2 text-sm text-primary underline"><Download size={14} /> {item.attachmentName || "Preview / download attachment"}</a> : <div className="mt-2 text-sm text-muted-foreground">— no attachment —</div>}</div>{(item.attachments ?? []).length > 0 && <div className="mt-6 border-t border-[#d9d7d1] pt-5"><div className="font-mono text-[9px] uppercase tracking-[.15em] text-primary">Uploaded experiment evidence</div><div className="mt-3 grid gap-2 md:grid-cols-2">{(item.attachments ?? []).map((file) => <a key={file.id} href={file.url} target="_blank" rel="noreferrer" className="flex items-center gap-2 border border-[#d9d7d1] bg-white p-3 text-sm text-primary underline"><Download size={14} /><span className="min-w-0 flex-1 truncate">{file.fileName}</span><span className="font-mono text-[9px] uppercase tracking-[.1em] text-muted-foreground">{file.kind}</span></a>)}</div></div>}</div></div><div className="mt-8 border-t border-[#d9d7d1] pt-6"><div className="flex flex-wrap gap-3"><button onClick={() => onApprove(item.discipline || "Social Psychology")} disabled={approving} className="focus-ring flex items-center gap-2 bg-[#3f7b44] px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-white disabled:opacity-50"><Check size={14} /> {approving ? "Publishing…" : "Approve & Publish"}</button><button onClick={() => setRejectId(rejectId === item.id ? null : item.id)} className="focus-ring flex items-center gap-2 border border-[#c58e8e] bg-[#fff5f5] px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-[#8a2c2c]"><X size={14} /> Reject & Send back</button><button onClick={() => beginEdit(item)} className="focus-ring flex items-center gap-2 border border-border bg-white px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-ink"><Edit3 size={14} /> Edit</button></div>{rejectId === item.id && <div className="mt-4 border border-[#d9a7a7] bg-[#fff1f1] p-4"><label className="font-mono text-[9px] uppercase tracking-[.13em] text-[#8a2c2c]">Permanent rejection comment<textarea value={rejectComment} onChange={(event) => setRejectComment(event.target.value)} className="form-control mt-1 min-h-24 resize-y" placeholder="Explain what should change before resubmission..." /></label><button onClick={onReject} disabled={!rejectComment.trim() || rejecting} className="focus-ring mt-3 bg-[#8a2c2c] px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-white">{rejecting ? "Saving…" : "Confirm rejection"}</button></div>}{editId === item.id && <form onSubmit={submitEdit} className="mt-4 border border-[#9fb0c4] bg-[#eef4f9] p-4"><div className="flex items-center gap-2 font-mono text-[9px] uppercase tracking-[.15em] text-primary"><Edit3 size={14} /> Full-form editing view / original preserved</div><div className="mt-4 grid gap-4 md:grid-cols-2"><AdminField label="Member name" value={editForm.memberName} onChange={(value) => setEditValue("memberName", value)} /><AdminField label="Member ID" value={editForm.memberId} onChange={(value) => setEditValue("memberId", value)} /><AdminField label="Academic category" value={editForm.discipline} onChange={(value) => setEditValue("discipline", value)} select options={["", ...libraryDisciplines]} /><AdminField label="Experiment / idea title" value={editForm.title} onChange={(value) => setEditValue("title", value)} /><AdminField label="One-sentence summary" value={editForm.abstract} onChange={(value) => setEditValue("abstract", value)} textarea /><AdminField label="Research lifecycle" value={editForm.lifecycle} onChange={(value) => setEditValue("lifecycle", value)} select options={["idea", "design", "in_progress", "completed"]} /><AdminField label="Theoretical basis" value={editForm.theoreticalBasis} onChange={(value) => setEditValue("theoreticalBasis", value)} textarea /><AdminField label="Creator & historical background" value={editForm.historicalBackground} onChange={(value) => setEditValue("historicalBackground", value)} textarea /><AdminField label="Research hypothesis" value={editForm.hypothesis} onChange={(value) => setEditValue("hypothesis", value)} textarea /><AdminField label="Proposed procedure" value={editForm.procedure} onChange={(value) => setEditValue("procedure", value)} textarea /><AdminField label="Materials & environment" value={editForm.materials} onChange={(value) => setEditValue("materials", value)} textarea /><AdminField label="Expected academic output" value={editForm.expectedOutput} onChange={(value) => setEditValue("expectedOutput", value)} /></div><label className="focus-ring mt-4 flex cursor-pointer items-center gap-3 border border-dashed border-[#9fb0c4] bg-white px-4 py-3 text-sm text-muted-foreground hover:border-primary hover:text-primary"><FileUp size={16} /><span>{editForm.attachmentData ? `Replacement: ${editForm.attachmentName}` : item.attachmentName ? `Replace attached file: ${item.attachmentName}` : "Add an optional attachment"}</span><input type="file" accept=".pdf,.doc,.docx,.txt" className="sr-only" onChange={(event) => { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => { setEditValue("attachmentName", file.name); setEditValue("attachmentMimeType", file.type || "application/octet-stream"); setEditValue("attachmentData", typeof reader.result === "string" ? reader.result : ""); }; reader.readAsDataURL(file); }} /></label><div className="mt-4 flex flex-wrap gap-3"><button type="submit" disabled={editing} className="focus-ring bg-primary px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-white">{editing ? "Saving edit…" : "Append edit to history"}</button><button type="button" onClick={() => setEditId(null)} className="focus-ring border border-border bg-white px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-ink">Cancel</button></div></form>}<div className="mt-6 border-t border-[#d9d7d1] pt-5"><div className="flex items-center gap-2 font-mono text-[9px] uppercase tracking-[.15em] text-primary"><History size={13} /> Full iteration history</div>{historyForItem.length === 0 ? <p className="mt-3 text-xs text-muted-foreground">Loading history…</p> : <div className="mt-3 grid gap-2 md:grid-cols-2">{historyForItem.map((record) => <div key={record.id} className="border border-[#d9d7d1] bg-white p-3"><div className="flex justify-between gap-3 font-mono text-[9px] uppercase tracking-[.12em] text-primary"><span>{record.action}</span><span>{new Date(record.createdAt).toLocaleString()}</span></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{record.note || "Snapshot retained."}</p><details className="mt-3 border-t border-border pt-2"><summary className="cursor-pointer font-mono text-[9px] uppercase tracking-[.12em] text-primary">View retained snapshot</summary><pre className="mt-2 max-h-72 overflow-auto whitespace-pre-wrap bg-[#f5f2eb] p-3 text-[10px] leading-5 text-ink">{formatSnapshot(record.snapshotJson)}</pre></details></div>)}</div>}</div></div></div>}</article>;
}

function ExperimentAdminPanel() {
  const utils = trpc.useUtils();
  const experiments = trpc.experiments.list.useQuery();
  const remove = trpc.admin.deleteExperiment.useMutation({ onSuccess: (item) => { setSelectedId(null); setConfirmTitle(""); setMessage(item ? `Deleted experiment #${item.id}: ${item.title}` : "Experiment deleted."); void utils.experiments.list.invalidate(); }, onError: (error) => setMessage(`Error: ${error.message}`) });
  const [search, setSearch] = useState("");
  const [disciplineFilter, setDisciplineFilter] = useState("");
  const [categoryFilter, setCategoryFilter] = useState("");
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [confirmTitle, setConfirmTitle] = useState("");
  const [message, setMessage] = useState("");
  const filtered = (experiments.data ?? []).filter((item) => {
    const matchesSearch = [String(item.id), item.title, item.category, item.author, item.discipline].join(" ").toLowerCase().includes(search.trim().toLowerCase());
    const matchesDiscipline = !disciplineFilter || item.discipline === disciplineFilter;
    const matchesCategory = !categoryFilter || item.category === categoryFilter;
    return matchesSearch && matchesDiscipline && matchesCategory;
  });

  return <section className="mb-7 border border-[#d9a7a7] bg-[#fffafa] p-5 md:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-[#8a2c2c]">Danger zone / Published experiments</div><h2 className="mt-2 font-display text-2xl">Manage public records</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Administrators can permanently remove a public experiment. This action requires typing the exact title and does not delete the separate member submission history.</p></div><div className="font-mono text-[10px] uppercase tracking-[.13em] text-muted-foreground">{experiments.data?.length ?? 0} records</div></div><label className="mt-5 block"><span className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground">Find by ID, title, author, category, or discipline</span><input value={search} onChange={(event) => setSearch(event.target.value)} className="form-control mt-1" placeholder="Search title or author" /></label><div className="mt-3 grid gap-3 md:grid-cols-2"><label><span className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground">Big category / discipline</span><select value={disciplineFilter} onChange={(event) => setDisciplineFilter(event.target.value)} className="form-control mt-1"><option value="">All four disciplines</option>{libraryDisciplines.map((discipline) => <option key={discipline} value={discipline}>{discipline}</option>)}</select></label><label><span className="font-mono text-[9px] uppercase tracking-[.13em] text-muted-foreground">Small category / folder</span><select value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)} className="form-control mt-1"><option value="">All three folders</option>{libraryFolders.map((folder) => <option key={folder} value={folder}>{folder}</option>)}</select></label></div>{message && <div role={message.startsWith("Error:") ? "alert" : "status"} className={`mt-4 border px-4 py-3 text-sm ${message.startsWith("Error:") ? "border-[#d9a7a7] bg-[#fff1f1] text-[#8a2c2c]" : "border-[#b8ccb5] bg-[#edf5eb] text-[#3f7b44]"}`}>{message}</div>}<div className="mt-5 space-y-3">{experiments.isLoading ? <div className="font-mono text-[10px] uppercase tracking-[.14em] text-muted-foreground" role="status">Loading public records…</div> : filtered.length === 0 ? <div className="border border-dashed border-[#d9a7a7] p-5 text-sm text-muted-foreground">No public experiment matches this search.</div> : filtered.map((item) => <div key={item.id} className="border border-border bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="font-mono text-[9px] uppercase tracking-[.14em] text-primary">#{item.id} · {item.category}</div><div className="mt-1 font-display text-lg">{item.title}</div><div className="mt-1 text-xs text-muted-foreground">{item.author || "No author recorded"} · {item.discipline}</div></div><button onClick={() => { setSelectedId(selectedId === item.id ? null : item.id); setConfirmTitle(""); }} className="focus-ring border border-[#c58e8e] px-3 py-2 font-mono text-[10px] uppercase tracking-[.13em] text-[#8a2c2c] hover:bg-[#fff1f1]">{selectedId === item.id ? "Cancel delete" : "Delete"}</button></div>{item.category === "Completed Experimental Projects" && <ExperimentMediaUploader experimentId={item.id} />}{selectedId === item.id && <div className="mt-4 border-t border-[#ead0d0] pt-4"><label className="block text-sm text-[#8a2c2c]">Type the exact title <input autoFocus value={confirmTitle} onChange={(event) => setConfirmTitle(event.target.value)} className="form-control mt-1" placeholder={item.title} /></label><button disabled={confirmTitle !== item.title || remove.isPending} onClick={() => remove.mutate({ id: item.id, confirmTitle })} className="focus-ring mt-3 bg-[#8a2c2c] px-4 py-3 font-mono text-[10px] uppercase tracking-[.13em] text-white disabled:cursor-not-allowed disabled:opacity-40">{remove.isPending ? "Deleting…" : "Permanently delete this experiment"}</button></div>}</div>)}</div></section>;
}

function ExperimentMediaUploader({ experimentId }: { experimentId: number }) {
  const upload = trpc.admin.uploadExperimentAttachments.useMutation();
  const [message, setMessage] = useState("");
  const [preparing, setPreparing] = useState(false);
  const handleFiles = (event: React.ChangeEvent<HTMLInputElement>) => {
    const files = Array.from(event.target.files ?? []);
    if (!files.length) return;
    if (files.length > 8) { setMessage("Choose no more than 8 files at a time."); return; }
    setPreparing(true); setMessage("");
    Promise.all(files.map((file) => new Promise<{ fileName: string; data: string; mimeType: string; sizeBytes: number; kind: "photo" | "data" | "report" | "protocol" | "other" }>((resolve, reject) => {
      if (file.size > 12 * 1024 * 1024) { reject(new Error(`${file.name} is larger than 12 MB.`)); return; }
      const kind = file.type.startsWith("image/") ? "photo" : /csv|json|excel|spreadsheet|zip/.test(file.type) ? "data" : /pdf|word|document/.test(file.type) ? "report" : "other";
      const reader = new FileReader();
      reader.onload = () => resolve({ fileName: file.name, data: typeof reader.result === "string" ? reader.result : "", mimeType: file.type || "application/octet-stream", sizeBytes: file.size, kind });
      reader.onerror = () => reject(new Error(`Could not read ${file.name}.`));
      reader.readAsDataURL(file);
    }))).then((prepared) => upload.mutate({ experimentId, files: prepared }, { onSuccess: (result) => setMessage(`${result?.count ?? prepared.length} file(s) added to this completed project.`), onError: (error) => setMessage(`Error: ${error.message}`) })).catch((error: Error) => setMessage(`Error: ${error.message}`)).finally(() => { setPreparing(false); event.target.value = ""; });
  };
  return <div className="mt-4 border border-[#b8ccb5] bg-[#edf5eb] p-4"><div className="font-mono text-[9px] uppercase tracking-[.15em] text-[#3f7b44]">Completed project evidence</div><p className="mt-2 text-xs leading-5 text-muted-foreground">Add experiment photos, process data, protocols, or reports. These files become visible in the public completed-project gallery.</p><label className="focus-ring mt-3 flex cursor-pointer items-center gap-2 border border-dashed border-[#7aa67d] bg-white px-3 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-[#3f7b44] hover:border-primary"><FileUp size={14} /> {preparing || upload.isPending ? "Uploading…" : "Upload photos / data"}<input type="file" multiple accept="image/*,.csv,.json,.xlsx,.xls,.zip,.pdf,.doc,.docx,.txt" className="sr-only" onChange={handleFiles} /></label>{message && <div role={message.startsWith("Error:") ? "alert" : "status"} className={`mt-3 text-xs ${message.startsWith("Error:") ? "text-[#8a2c2c]" : "text-[#3f7b44]"}`}>{message}</div>}</div>;
}
function RecordField({ label, value }: { label: string; value: string | null }) { return <div><div className="font-mono text-[9px] uppercase tracking-[.15em] text-primary">{label}</div><div className="mt-2 whitespace-pre-wrap text-sm leading-6 text-ink">{value?.trim() || "— blank —"}</div></div>; }

function formatSnapshot(value: string) {
  try {
    return JSON.stringify(JSON.parse(value), null, 2);
  } catch {
    return value;
  }
}

function AdminField({ label, value, onChange, textarea, select, options }: { label: string; value: string; onChange: (value: string) => void; textarea?: boolean; select?: boolean; options?: string[] }) { return <label className={`block ${textarea ? "md:col-span-2" : ""}`}><span className="font-mono text-[9px] uppercase tracking-[.13em] text-ink">{label}</span>{textarea ? <textarea value={value} onChange={(event) => onChange(event.target.value)} className="form-control mt-1 min-h-24 resize-y" /> : select ? <select value={value} onChange={(event) => onChange(event.target.value)} className="form-control mt-1">{options?.map((option) => <option key={option || "blank"} value={option}>{option || "Not selected"}</option>)}</select> : <input value={value} onChange={(event) => onChange(event.target.value)} className="form-control mt-1" />}</label>; }
