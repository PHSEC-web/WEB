CREATE TABLE `attachments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`recordId` int NOT NULL,
	`revisionId` int,
	`fileName` varchar(240) NOT NULL,
	`storageKey` varchar(320) NOT NULL,
	`mimeType` varchar(160) NOT NULL,
	`sizeBytes` int,
	`kind` enum('report','protocol','data','photo','other') NOT NULL DEFAULT 'other',
	`visibility` enum('public','members') NOT NULL DEFAULT 'members',
	`uploadedByOpenId` varchar(64),
	`uploadedByName` varchar(160),
	`deletedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `attachments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `recordRevisions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`recordId` int NOT NULL,
	`revisionNo` int NOT NULL,
	`action` varchar(60) NOT NULL,
	`summary` varchar(240) NOT NULL,
	`changedFields` json NOT NULL,
	`snapshotJson` text NOT NULL,
	`editorOpenId` varchar(64),
	`editorName` varchar(160),
	`editorRole` enum('member','admin','system') NOT NULL DEFAULT 'system',
	`note` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `recordRevisions_id` PRIMARY KEY(`id`),
	CONSTRAINT `record_revisions_record_revision_unique` UNIQUE(`recordId`,`revisionNo`)
);
--> statement-breakpoint
CREATE TABLE `records` (
	`id` int AUTO_INCREMENT NOT NULL,
	`slug` varchar(160) NOT NULL,
	`recordKind` enum('reference','project') NOT NULL DEFAULT 'project',
	`discipline` varchar(80) NOT NULL,
	`category` varchar(100) NOT NULL,
	`status` enum('pending','needs_revision','published','rejected','hidden') NOT NULL DEFAULT 'pending',
	`lifecycle` enum('idea','design','in_progress','completed'),
	`visibility` enum('public','members') NOT NULL DEFAULT 'public',
	`title` varchar(240) NOT NULL,
	`abstract` text,
	`theoreticalBasis` text,
	`historicalBackground` text,
	`hypothesis` text,
	`procedure` text,
	`materials` text,
	`expectedOutput` varchar(120),
	`results` text,
	`limitations` text,
	`nextQuestion` text,
	`ethicsReviewed` int NOT NULL DEFAULT 0,
	`ethicsNotes` text,
	`authorName` varchar(160),
	`authorMemberId` varchar(80),
	`ownerOpenId` varchar(64),
	`forkOfRecordId` int,
	`reviewComment` text,
	`requiredChanges` json,
	`reviewedBy` varchar(160),
	`reviewedAt` timestamp,
	`publishedAt` timestamp,
	`revisionCount` int NOT NULL DEFAULT 1,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	`deletedAt` timestamp,
	CONSTRAINT `records_id` PRIMARY KEY(`id`),
	CONSTRAINT `records_slug_unique` UNIQUE(`slug`),
	CONSTRAINT `records_discipline_category_status_idx` UNIQUE(`discipline`,`category`,`status`,`id`)
);
